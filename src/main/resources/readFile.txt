Для задания надо сделать maven-проект. (File -> Project -> Maven -> (Указать имя проекта) -> Finish.
Если папки Java и Resources не пометились автоматически, как Sources root и Resources Root соответственно,
пометить их самостоятельно, для этого вызвать контекстное меню, там выбрать пункт Mark Directory As,
откроются варианты для выбора)